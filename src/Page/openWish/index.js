import { OpenWishList, Details } from '../../Components/openWish/'

// This is a common component for new wish, in review, waiting for approval

export default {
    list: OpenWishList,
    show: Details,
};
